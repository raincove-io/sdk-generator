version: 2.1
jobs:
  build:
    docker:
      - image: alpine:3.7
    steps:
      - run: |
          docker build . -t erfangc/sdk-generator:latest
workflows:
  version: 2
  default:
    jobs:
      - build:
          filters:
            branches:
              only: master
        